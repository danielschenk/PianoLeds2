#!python

import os
join = os.path.join

Import('base_env')
env = base_env.Clone()

env.Append(CPPPATH=[join('Drivers', 'PC')])

env.Program('MidiInputMonitor', Glob('*.cpp') + env.GetComponents('Drivers', 'RtMidi', 'Common'))
