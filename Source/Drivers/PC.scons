#!python

Import('base_env')
env = base_env.Clone()

env.Append(CPPPATH=['Interfaces', 'PC'])

sources = Glob('PC/*.cpp') + Glob('Common/*.cpp')

lib = env.Library('Drivers', sources)
base_env.Component('Drivers', lib)
